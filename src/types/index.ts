export interface HeadingProps {
	title: string;
}

export interface InputProps {
	type: string;
	name: string;
	autocomplete: string;
	placeholder: string;
}

export interface CustomButtonProps {
	title: string;
}
